<UserControl x:Class="LauncherBartolome.Views.AplicacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">

            <!-- TITULO -->
            <Border Background="#1C1C1C"
                    CornerRadius="10"
                    Padding="15"
                    Margin="0,0,0,20">
                <TextBlock Text="Aplicaciones"
                           Foreground="White"
                           FontSize="22"
                           FontWeight="SemiBold"/>
            </Border>

            <!-- BUSCADOR -->
            <Border Background="#1C1C1C"
                    CornerRadius="10"
                    Padding="10"
                    Margin="0,0,0,15"
                    BorderBrush="#222"
                    BorderThickness="1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="🔍"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"
                               Margin="2,0,0,0"/>

                    <Grid Grid.Column="1">
                        <TextBox x:Name="SearchBox"
                                 Height="34"
                                 VerticalContentAlignment="Center"
                                 Foreground="White"
                                 Background="#2A2A2A"
                                 BorderBrush="#333"
                                 TextChanged="SearchBox_TextChanged"
                                 Padding="10,0"
                                 KeyDown="SearchBox_KeyDown"/>

                        <TextBlock Text="Buscar aplicaciones..."
                                   Foreground="#888888"
                                   Margin="14,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=SearchBox, Path=Text.Length}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>
            </Border>

            <!-- APPS ARRIBA -->
            <Grid Margin="0,0,0,10">
                <ItemsControl x:Name="AppsItems" ItemsSource="{Binding Apps}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button
                                Command="{Binding DataContext.OpenAppCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0"
                                Margin="0,0,12,0"
                                Cursor="Hand"
                                FocusVisualStyle="{x:Null}">

                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Button.Template>

                                <Border Style="{StaticResource LauncherCardStyle}"
                                        Padding="12"
                                        Width="135"
                                        Height="105">
                                    <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                        <Image Source="{Binding Icon}"
                                               Width="36"
                                               Height="36"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,8"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>

                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="White"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   TextAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   HorizontalAlignment="Center"
                                                   MaxWidth="100"/>

                                        <TextBlock Text="{Binding ExecutablePath}"
                                                   Visibility="Collapsed"/>
                                    </StackPanel>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No hay resultados para tu búsqueda."
                           Foreground="#AAAAAA"
                           FontSize="13"
                           TextAlignment="Center"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Margin="20"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=AppsItems, Path=Items.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- BANNER DEBAJO -->
            <Button Background="Transparent"
        BorderThickness="0"
        Padding="0"
        Margin="0,0,0,10"
        Cursor="Hand"
        FocusVisualStyle="{x:Null}"
        Command="{Binding OpenBannerLinkCommand}">

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="OuterBorder"
                    Background="#1C1C1C"
                    CornerRadius="14"
                    Padding="12"
                    BorderBrush="#303030"
                    BorderThickness="1">

                            <Border CornerRadius="10"
                        ClipToBounds="True">
                                <Image x:Name="BannerImage"
                           Source="{Binding CurrentBannerImage}"
                           Height="180"
                           Stretch="UniformToFill"/>
                            </Border>
                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder"
                            Property="BorderBrush"
                            Value="#505050"/>
                                <Setter TargetName="BannerImage"
                            Property="Opacity"
                            Value="0.9"/>
                            </Trigger>

                            <!-- CLICK -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BannerImage"
                            Property="Opacity"
                            Value="0.8"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </ScrollViewer>

</UserControl>